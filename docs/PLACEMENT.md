# Artifact Placement Map

- Backend and server code: `services/synthia-server/`
- Core network surface: `apps/resonance-network/`
- Dashboard and OS shells: `apps/`
- Shared contracts and render engines: `packages/`
- Runtime substrate: `core/runtime/`
- Research and lab systems: `labs/`
- Model definitions: `models/`
- Production bundles: `deployments/`
- Older prototypes and duplicates: `archive/`

## Current priority

1. Put backend files in `services/synthia-server/`.
2. Put Resonance Network in `apps/resonance-network/` and wire it as a core surface.
3. Put Stellar Proximology guide and lab notes in `labs/stellar-proximology/`.
4. Put reusable glyph/state/event types in `packages/`.
5. Put runtime/remount artifacts in `core/runtime/`.
